name: Update Dependencies
on:
  schedule:
    - cron: "* */6 * * *"
  workflow_dispatch:
jobs:
  niv-update:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dep:
          - home-manager
          - nixos-hardware
          - nixpkgs
          - nixus
          - sops-nix
          - pre-commit-hooks
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Nix
        uses: cachix/install-nix-action@v13
        with:
          nix_path: nixpkgs=channel:nixos-unstable-small

      - name: Setup Cachix
        uses: cachix/cachix-action@v10
        with:
          name: nix-community
          extraPullNames: cpcloud-nix-config

      - name: Get current commit
        id: get_current_commit
        run: echo ::set-output name=sha256::$(./get_dep_info.sh ${{ matrix.dep }} rev)

      - name: Update ${{ matrix.dep }}
        run: nix run nixpkgs.niv -c niv update ${{ matrix.dep }}

      - name: Get new commit
        id: get_new_commit
        run: echo ::set-output name=sha256::$(./get_dep_info.sh ${{ matrix.dep }} rev)

      - name: Get repository owner
        id: repo_owner
        run: echo ::set-output name=value::$(./get_dep_info.sh ${{ matrix.dep }} owner)

      - name: Get repository name
        id: repo_name
        run: echo ::set-output name=value::$(./get_dep_info.sh ${{ matrix.dep }} repo)

      - name: Get log lines for upstream changes
        uses: cpcloud/compare-commits-action@v2.0.4
        id: compare_commits
        if: ${{ steps.get_current_commit.outputs.sha256 != steps.get_new_commit.outputs.sha256 }}
        with:
          owner: ${{ steps.repo_owner.outputs.value }}
          repo: ${{ steps.repo_name.outputs.value }}
          basehead: ${{ steps.get_current_commit.outputs.sha256 }}...${{ steps.get_new_commit.outputs.sha256 }}
          token: ${{ secrets.GITHUB_TOKEN }}
          show-merge-commits: false

      - name: Create a pull request to update ${{ matrix.dep }}
        if: ${{ steps.get_current_commit.outputs.sha256 != steps.get_new_commit.outputs.sha256 }}
        uses: peter-evans/create-pull-request@v3
        with:
          commit-message: "chore(deps): update ${{ matrix.dep }}"
          branch: "create-pull-request/update-${{ matrix.dep }}"
          delete-branch: true
          author: "Phillip Cloud <417981+cpcloud@users.noreply.github.com>"
          title: "chore(deps): update ${{ matrix.dep }}"
          token: ${{ secrets.PR }}
          body: ${{ steps.compare_commits.outputs.differences }}
